App As appinfo:
    BackEnabled: =false
    OnStart: |+
        =Collect(BudgetsCollect, Budgets)
        
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

